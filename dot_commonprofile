# shellcheck shell=bash
source "$HOME/.commonfuncs"

# run the fortune function....and...laugh
# only run in interactive shells to avoid output in non-interactive contexts like Claude Code
if [ -t 1 ] && [ -z "$NO_LAUGHS" ]; then
    makeMeLaugh
fi

# only update once a day
# if checkPath git && [ -d ~/.dotfiles/.git ] && weShouldUpdate; then
#     (cd ~/.dotfiles && git up)
# fi

if checkPath keychain && [ -f ~/.ssh/id_rsa ]; then
    eval "$(keychain -q --eval id_rsa)"
fi
